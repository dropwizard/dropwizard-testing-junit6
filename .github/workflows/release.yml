---
name: Release
# yamllint disable-line rule:truthy
on:
  push:
    tags:
      - dropwizard-testing-junit6-*
jobs:
  release:
    uses: dropwizard/workflows/.github/workflows/release.yml@91afe30da07f7fc632020064a53f2c5ac0f4be9b # main
    secrets: inherit
